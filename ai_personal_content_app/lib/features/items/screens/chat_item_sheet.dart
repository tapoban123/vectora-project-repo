import 'package:flutter/material.dart';

class ChatItemSheet extends StatelessWidget {
  const ChatItemSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
